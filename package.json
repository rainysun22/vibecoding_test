{
  "name": "openwork",
  "version": "0.1.0",
  "private": true,
  "description": "OpenWork — 开源中立、成果交付、本地可控的 All-in-One 个人 AI 工作台",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @openwork/types && npm run build -w @openwork/llm-gateway && npm run build -w @openwork/deliverables && npm run build -w @openwork/core && npm run build -w @openwork/server",
    "build:web": "npm run build -w @openwork/web",
    "dev": "npm run dev -w @openwork/server",
    "dev:web": "npm run dev -w @openwork/web",
    "start": "npm run start -w @openwork/server",
    "test": "npm run test -w @openwork/llm-gateway && npm run test -w @openwork/deliverables && npm run test -w @openwork/core",
    "typecheck": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7"
  }
}
