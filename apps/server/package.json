{
  "name": "@openwork/server",
  "version": "0.1.0",
  "description": "OpenWork API 服务器：本地优先 Fastify 服务 + SSE 实时流",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput & node --watch dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@openwork/core": "0.1.0",
    "@openwork/types": "0.1.0",
    "@openwork/llm-gateway": "0.1.0",
    "@openwork/deliverables": "0.1.0",
    "fastify": "^5.2.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/static": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.0"
  }
}
